---
name: deeptalk
description: Use when 사용자가 결론·구현을 강요하지 않고 의견·관점·트레이드오프를 교환하려 할 때 — "어떻게 생각해?", "니 의견은?", "이거 맞아?", "왜 그래야 해?", "같이 생각해보자", "논의하자", "/deep-talk", "/hams:deeptalk" 같은 자연 트리거 포함. 코드 수정 없이 주제를 탐색.
allowed-tools:
  - Read
  - Glob
  - Grep
  - Write
  - AskUserQuestion
  - Bash
---

# /hams:deeptalk

코드를 건드리지 않고 주제를 깊이 탐색하는 대화 스킬. 의견 교환, 기술 선택 비교, 아키텍처 논의, 문제 분석에 사용.

> **`project_root`**: 현재 git 루트 디렉토리 (`git rev-parse --show-toplevel` 결과). git 외부면 `cwd`.

## HARD-GATE

이 스킬 진행 중 **코드/설정 파일 수정, 신규 파일 생성, 구현 작업을 절대 하지 않는다**. 예외는 종료 시 사용자가 승인한 경우의 대화 요약 문서(`docs/discussions/...`) 한 건뿐.

읽기 전용 탐색(Read/Glob/Grep)은 허용. 코드 수정 요청이 들어오면 **이 스킬을 종료한다**. 자동으로 다음 작업으로 넘어가지 않는다 — 종료 후 사용자가 직접 다음 단계를 지시하도록 한다.

## 활성화 절차

스킬이 발동되면 **반드시 먼저** baby-hamster 기록을 차단:

```bash
mkdir -p .hamstern && touch .hamstern/.deeptalk-running
```

이 마커가 있는 동안 hamstern hook은 user prompt와 stop 메시지를 baby-hamster에 기록하지 않는다. deep-talk 내용은 결정사항이 아니므로 mom/boss 분석 노이즈가 되지 않게 한다.

`.hamstern/` 디렉토리가 없는 프로젝트(hamstern 비활성)에서는 마커 생성을 건너뛴다 — 어차피 기록되지 않음.

## brainstorming, gsd-explore와의 차이

|              | deeptalk          | brainstorming      | gsd-explore       |
|--------------|-------------------|--------------------|-------------------|
| 목적         | 탐색, 이해, 의견 교환 | 기능 설계 → 구현 전환 | 아이디어 라우팅    |
| 코드 수정    | X (HARD-GATE)     | 설계 후 구현으로     | X                 |
| 결과물       | 선택적 요약 문서   | 설계 스펙 → 플랜    | 라우팅 결정       |
| 종료 시점    | 사용자 종료 신호   | writing-plans 호출  | 구체 명령으로 전환 |

## 체크리스트

**필수 (1~3)** — 모든 deep-talk:
1. **주제 파악** — 사용자가 무엇을 알고 싶은지/논의하고 싶은지 이해
2. **맥락 탐색 (필요시)** — 관련 코드/문서 읽기 전용 탐색
3. **깊이 있는 대화** — 한 번에 하나의 질문, 단계적으로 깊이 들어감

**조건부 (4~6)** — 결정/합의 도출이 목표일 때만:
4. **접근법 제시** — 2~3가지 방향 + 트레이드오프 + 추천
5. **합의 도출** — 사용자와 방향 합의
6. **요약 저장** — 결론·결정·향후 행동이 도출된 경우만 (아래 참고)

순수 의견 교환·잡담은 1~3에서 자연스럽게 끝낸다. 매 대화마다 4~6 강제하면 brainstorming 화 됨.

## 질문 스타일 분기

| 상황 | 도구 | 예시 |
|------|------|------|
| **사고 자극·관점 끌어내기** | 열린 질문 (Socratic) | "왜 그렇게 생각하세요?", "다른 관점에서 보면?" |
| **결정·선택 시점** | `AskUserQuestion` 객관식 | "A vs B 중 어느 쪽?" |
| **트레이드오프 비교** | 표 + 짧은 설명 | (좌우 비교 마크다운 표) |

소크라테스식과 객관식이 충돌하는 게 아니라 **단계가 다르다** — 사고를 끌어낼 때는 열린 질문, 수렴할 때는 객관식.

## 대화 원칙

- **한 번에 하나의 질문** — 여러 질문을 한꺼번에 던지지 않는다
- **가정하지 않는다** — 모르면 물어본다
- **깊이 우선** — 넓게 퍼지지 말고 하나의 주제를 끝까지 파고든다
- **시각적 설명** — 표·다이어그램·비교표를 적극 활용
- **반론 제기 OK** — 사용자 의견에 동의 안 되면 정중하게 반대하고 근거 제시

## 종료 절차

다음 신호가 오면 종료:
- 명시적: "이 정도면 됐어", "고마워", "끝", "다른 거 하자", "/hams:deeptalk:end"
- 자연스러운: 결론에 도달하고 추가 질문 없음, 사용자가 다른 작업으로 전환 시도

종료 시 **반드시**:
1. 마커 제거: `rm -f .hamstern/.deeptalk-running`
2. 저장 가치 판단 (아래 섹션)
3. 사용자에게 명시적 종료 안내 — 자동으로 다음 작업으로 넘어가지 않는다.

## 요약 저장 기준

**저장한다** (아래 중 하나라도 해당):
- 결론·결정사항이 도출됨 (예: "A 방식 채택하기로 함")
- 향후 행동 항목이 생김 (예: "다음 주에 X 검토")
- 트레이드오프 분석 자료가 충분히 축적됨 (재참조 가치)

**저장하지 않는다**:
- 단순 의견 교환·잡담
- 결론 없이 "재미있는 얘기였다" 정도로 끝남
- 사용자가 명시적으로 저장 거부

저장 전 사용자에게 **반드시 확인**:
> "결론·다음 단계가 정리됐어요. `docs/discussions/{YYYY-MM-DD}-{slug}.md`에 저장할까요?"

승인 시 다음 형식으로 저장:

```markdown
# {주제}

- 날짜: {YYYY-MM-DD}
- 참여: 사용자 + Claude

## 배경
{왜 이 대화를 했는지}

## 핵심 논의
{주요 질문과 답변 요약}

## 결론
{합의된 방향 또는 결정사항}

## 다음 단계
{구현/검토가 필요하면 어떤 작업이 필요한지}
```

저장은 `docs/discussions/` (프로젝트 루트 기준)이며, 이는 hamstern hook이 캡처하지 않는 일반 파일 쓰기이므로 baby-hamster에 영향 없음.

## Red Flags — 이 스킬에서 빠져나가야 할 신호

다음 신호가 보이면 즉시 deep-talk을 종료한다:

- "이거 구현해줘" / "코드 짜줘"
- "리팩토링해줘" / "수정해줘"
- "이 버그 고쳐줘"
- "테스트 추가해줘"
- 그 외 명시적 코드 수정 요청

**종료 절차**:
1. `.hamstern/.deeptalk-running` 마커 제거 (`rm -f .hamstern/.deeptalk-running`)
2. 사용자에게 명시적 종료 안내 — "deep-talk 종료합니다. 코드 작업으로 넘어가시려면 다음 메시지로 지시해 주세요."
3. **자동 라우팅 금지** — brainstorming/plan/debugging 등 후속 스킬을 Claude가 임의로 호출하지 않는다. 다음 작업의 진입은 사용자의 다음 메시지 또는 명시적 명령에서 시작.

**선택적 추천**: 종료 안내 시 available-skills 목록에 후속 작업에 적합한 스킬이 있다면 1줄 추천 가능 (강요 아님):
> "참고: 구현 설계가 필요하시면 `superpowers:brainstorming` → `superpowers:writing-plans` 흐름이 유용합니다."

추천 가능한 스킬이 없으면 추천 문구 생략. 사용자가 알아서 진행하도록 둠.

## 마커 누수 안전망

비정상 종료로 `.deeptalk-running` 마커가 남아도 mtime 24시간 초과 시 hook이 자동 정리한다 (`is_deeptalk_running` 함수의 stale-cleanup 로직). 별도 청소 스크립트 불필요.
